#include <gtest/gtest.h>

#include <greeter/greeter.hpp>

TEST(GreeterTest, GreeterTest) {
    // This test is a placeholder
    // It will be replaced by a real test
    EXPECT_EQ(Greeter::greet("World"), std::string("Hello, World!"));
}